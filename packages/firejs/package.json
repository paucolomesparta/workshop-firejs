{
	"name": "firejs",
	"version": "1.0.0",
	"description": "React-like reactive web framework for the modern web",
	"main": "dist/index.js",
	"scripts": {
		"build": "esbuild src/index.ts --bundle --platform=node --outdir=dist && tsc --emitDeclarationOnly",
		"build:watch": "esbuild src/index.ts --bundle --platform=node --outdir=dist --watch & tsc --emitDeclarationOnly --watch"
	},
	"dependencies": {
		"@emotion/is-prop-valid": "^1.2.1"
	},
	"devDependencies": {
		"esbuild": "^0.19.0",
		"typescript": "^4.8.4"
	}
}
